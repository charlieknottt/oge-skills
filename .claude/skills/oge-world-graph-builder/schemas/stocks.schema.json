{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oge/skills/world-graph-builder/stocks.schema.json",
  "title": "OGE Stock Model",
  "description": "The detailed model behind each graph node. Feeds the qualitative sidecar. Authors the resting value as 'node_value' on the 0-1 scale; it is the same number as a node's 'baseline' in the graph and runtime files. See schemas/runtime_graph.schema.json for the merged play-ready shape and guide/2-data-shapes.md.",
  "type": "object",
  "required": ["stocks"],
  "properties": {
    "scenario": { "type": "string" },
    "count": { "type": "integer" },
    "stocks": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/stock" }
    },
    "design_notes": { "type": "string" }
  },
  "additionalProperties": true,
  "$defs": {
    "stock": {
      "type": "object",
      "required": ["id", "name", "pmesii", "node_value", "inverted", "stock_type"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
        "name": { "type": "string", "minLength": 1 },
        "pmesii": {
          "type": "string",
          "enum": ["Political", "Military", "Economic", "Social", "Information", "Infrastructure", "Physical"],
          "description": "the fixed PMESII-P dimension (Physical = Physical Environment)"
        },
        "measures": { "type": "string", "description": "what the stock concretely measures" },
        "unit": { "type": "string", "description": "display unit + note on how it maps to the scale" },
        "node_value": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "resting value on the 0-1 scale; the same number as a node's baseline in the graph and runtime files."
        },
        "increases_when": { "type": "string" },
        "decreases_when": { "type": "string" },
        "rationale": { "type": "string" },
        "inverted": { "type": "boolean", "description": "true when a lower value is the good state (a threat or compromise stock)" },
        "stock_type": {
          "type": "string",
          "enum": ["zero-sum-pool", "scheduled-threat", "spendable", "service-level", "clock", "buildout", "trust-sentiment", "upstream-access", "policy-lever", "capacity", "other"],
          "description": "one of the 9 stock types from the questions in guide/1-how-the-model-works.md. The graph's node behavior (the 'type' field: Level/Lever/Accumulator/Drifter) is derived from this."
        },
        "sector": { "type": "string", "description": "scenario-specific grouping (free text)" }
      },
      "additionalProperties": true
    }
  }
}
